1.电路交换网络中的多路复用：频分多路复用(FDM链路的频谱由跨越链路创建的所有链接所共享)和时分多路复用(TDM时域被分割为帧，每个帧具有4个时隙，每个电路在循环的TDM帧中被分配相同的专用时隙)
2.分组交换网时延：节点处理时延、排队时延、传输时延、传播时延
3.拒绝服务(DOS)分为三种：弱点攻击、带宽洪泛、连接洪泛
4.运输层协议为应用程序提供的服务：可靠数据传输、吞吐量、定时、安全性
5.网络应用程序体系结构：客户机/服务器体系结构、对等体系结构(P2P)
6.Web应用是一种客户机/服务器应用程序，包括文档格式的标准(HTML)、Web浏览器、Web服务器、应用层协议
7.URL地址分为两部分：存放对象的服务器主机名和对象的路径名
8.因特网电子邮件系统：用户代理、邮件服务器、简单邮件传输协议
9.运输层和网络层之间的关系
  运输层为运行在不同主机上的进程之间提供了逻辑通信，运输层协议只工作在端系统中，它将来自应用进程的报文移动到网络层
  网络层提供了主机之间的逻辑通信，
  运输层协议所能提供的服务受到了底层网络层协议的服务模型的限制。如果网络层协议不能为两主机之间发送的运输层报文段提供时延和带宽保证，那么运输层协议也不能为两进程之间发送的报文提供时延和带宽保证。
  即使底层网络协议在网络层不提供相应服务，运输层协议也能提供某些服务，如网络层协议会使分组丢失、混乱和重复，运输层协议也能提供可靠的服务，即使网络层不能保证运输层报文段的机密性，运输层也能使用加密来确保应用层报文不被读取
10.套接字是应用程序进程和运输层协议之间的接口
11.运输层多路复用：套接字有唯一标识符  每个报文段有特殊字段(源端口号字段和目的端口号字段等)来指示该报文段所要交付的套接字
12.UDP套接字由目的IP地址和目的端口号的二元组来标识的
   TCP套接字由源IP地址、源端口号、目的IP地址、目的端口号
13.流量控制：消除发送方使接收方缓存溢出的可能性
   拥塞控制：发送方可能因为IP网络的拥塞而被遏制
14.TCP拥塞控制算法：加性增乘性减 慢启动 对超时事件作出反应
15.网络层功能：转发是指将一个分组从一个输入链路接口转移到适当的输出链路接口的路由器本地动作
               选路是指分组从源到目的地时，决定端到端路径的网络范围的进程
16.仅在网络层提供链接服务的计算机网络成为虚电路网络
   仅在网络层提供无连接服务的计算机网络成为数据报网络
17.运输层面向连接服务是在位于网络边缘的端系统中实现的，网络层连接服务除了在端系统中实现外也在位于网络核心的路由器实现
18.路由器体系结构：输入端口 输出端口 交换接口 选路处理器
19.网络层组件：IP协议 选路组件 报告数据报中的差错和对某些网络层信息请求进行响应的设施
20.网络层协议：IP协议，编制规则 数据报格式 分组处理规则
               ICMP协议，差错报告 路由器信令
21.网络层选路算法：全局/分布式     全局选路算法(链路状态算法LS)
			           分布式选路算法(距离向量算法DV)
                   静态/动态       静态选路算法
                                   动态选路算法
	           负载敏感/迟钝   负载敏感算法
				   负载迟钝算法


HTTP

1.绝对URI格式
  http://    user:pass     @ www.example.jp : 80/        dir/index.htm    ? uid=1     # ch1
  协议方案名 登录信息(认证)  服务器地址      服务器端口号 带层次的文件路径  查询字符串  片段字符串
2.http报文首部：通用首部、请求首部、响应首部、实体首部、请求行(方法，URI，HTTP版本)、状态行(HTTP版本，状态码)等
3.状态码类别
  1XX： Informational(信息性状态码)     接收的请求正在处理
  2XX： Success(成功状态码)             请求正常处理完毕
  3XX： Redirection(重定向状态码)       浏览器需要进行附加操作以完成请求
  4XX： Client Error(客户端错误状态码)  服务器无法处理请求
  5XX： Server Error(服务器错误状态码)  服务器处理请求出错

  200 OK 请求已被正常处理，返回资源
  204 No Content 请求处理成功，无资源返回
  206 Partial Content 表示客户端进行了范围请求，服务器成功执行，返回指定范围的内容
  301 Moved Permanently 永久性重定向，请求的资源已被分配了新的URI
  302 Found 临时性重定向，请求的资源已被分配了新的URI
  303 See Other 请求对应的资源存在着另一个URI，应使用GET方法定向获取请求的资源
  304 Not Modified 资源已找到但未符合请求附带的条件，返回时不包含任何响应的主体部分
  307 Temporary Redirect 与302相同
  400 Bad Request 请求报文中存在语法错误
  401 Unauthorized 表示发送的请求需要有通过HTTP认证的认证信息，若之前已进行过一次请求则表示用户认证失败
  403 Forbidden 请求资源的访问被服务器拒绝了
  404 Not Found 服务器无法找到请求的资源
  500 Internal Server Error 服务器端执行请求时发生了错误
  503 Service Unavailable 服务器暂时处于超负载或正在进行停机维护
4.HTTP使用的认证方式
  BASIC认证(基本认证)无加密
  DIGEST(摘要认证)采用质询/响应方式，防止密码被窃听
  SSL客户端认证
  FormBaes(基于表单认证)
5.使用SPDY协议后HTTP协议额外获得以下功能
  (1)多路复用流
     通过单一TCP连接，可以无限制处理多个HTTP请求，所有请求处理都在一条TCP连接上完成，SPDY只是将单个域名的通信多路复用
     所以当一个web网站使用多个域名下的资源，改善效果就会受到限制
  (2)赋予请求优先级
     SPDY不仅可以无限制的并发处理请求，还可以给请求逐个分配优先级顺序
  (3)压缩HTTP首部
     减少通信产生的数据包数量和发送的字节数
  (4)推送功能
     服务器可直接发送数据不必等待客户端的请求
  (5)服务器提示功能
     服务器可以主动提示客户端请求所需的资源
6.WebSocket协议
  由于是建立在HTTP基础上的协议，连接的发起方仍是客户端，一旦确立WebSocket通信连接，不论服务器还是客户端都可以向对方发送报文
  主要特点
  (1)推送功能
     服务器可直接向客户端推送数据
  (2)减少通信量
     和HTTP相比，每次连接时的总开销减少，而且WebSocket的首部信息很小，通信量也相应减少了，为了实现WebSocket通信
     在HTTP连接建立之后，需要完成一次握手的步骤，之后不再使用HTTP数据帧，采用WebSocket独立的数据帧
7.针对Web应用的攻击模式：主动攻击(通过直接访问Web应用，把攻击代码传入的攻击模式)、被动攻击(利用圈套策略执行攻击代码的攻击模式，不直接对Web应用发起攻击)
  以服务器为目标的主动攻击：直接针对服务器上的资源进行攻击，最具代表性的攻击为SQL注入攻击和OS命令注入攻击
  以服务器为目标的被动攻击：攻击者设置陷阱诱导用户触发，最具代表性的攻击是跨站脚本攻击XSS和跨站点请求伪造
  利用用户的身份攻击企业内部网络：利用被动攻击可发起对原本从互联网上无法直接访问的企业内网等网络的攻击
8.跨站脚本攻击XSS：通过存在安全漏洞的Web网站注册用户的浏览器内运行非法的HTML标签或JavaScript进行的一种的攻击

  

1.(1)浏览器搜索自身的DNS缓存
  (2)搜索操作系统自身的DNS缓存
  (3)读取本地的HOST文件
  (4)浏览器发起一个DNS的一个系统调用
  (5)浏览器获得域名对应的IP地址后发起HTTP三次握手
  (6)TCP/IP连接建立起来后，浏览器向服务器发送HTTP请求
  (7)服务器接收请求后，将数据返回给浏览器
  (8)浏览器解析渲染页面


db             数据库存储目录
models         数据库模型文件目录
public         公共文件目录(css、js、image...)
routers        路由文件目录
schemas        数据库结构文件目录
views          模板视图文件目录
app.js         应用入口文件











































 